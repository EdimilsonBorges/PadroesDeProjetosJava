package Command;

public interface Command {

     /**
      * Este método executa os métodos do comando desejado
      * @param context recebe um contexto para o Toast
      */
     void execute();
}
